draw_self()
if original {
for (var i = 0; i < instance_number(obj_hook_holder);i += 1) {
		with instance_find(obj_hook_holder,i) {
			if pair_id = other.pair_id {
			other.pair_obj = id
			pair_obj = other.id
			original = false
			}
			
		}
}
}